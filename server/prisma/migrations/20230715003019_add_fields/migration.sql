-- CreateTable
CREATE TABLE "Turmas" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "turma" TEXT NOT NULL
);

-- CreateTable
CREATE TABLE "Horarios" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "horarios" TEXT NOT NULL
);
